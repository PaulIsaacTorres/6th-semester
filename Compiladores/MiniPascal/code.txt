program ejemplo1;

var

    a, x, y, b : int;

begin

    read (a, b);
    c := a * b;

    /*This is a comment*/
    
    if c >= 20 then
        if c >= 20 then
            write (c);
        else
            write (b);
    else
        while c < a do
            write (a);
end.
